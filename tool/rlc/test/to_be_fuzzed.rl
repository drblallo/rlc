import fuzzer.cpp_functions
import fuzzer.utils

act play():
	let current = 0
	while current != 7:
	    act subact(Int x)
	    current = x

	    actions:
	        act this(Int y)
	        current = y
	        act that()

fun main() -> Int:
	return 0
